// ============================================================
//  Smart Doorbell – Secrets TEMPLATE
//  Copy this file to secrets.h (same folder) and fill in the
//  real values. secrets.h is gitignored and never committed.
// ============================================================
#pragma once

#define SECRET_WIFI_SSID     "your-wifi-name"
#define SECRET_WIFI_PASSWORD "your-wifi-password"
#define SECRET_BOT_TOKEN     "123456789:AAxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
#define SECRET_CHAT_ID       "123456789"

// SEC-03: shared secret for backend device auth. Must match the backend's
// DEVICE_SECRET env var (agree the value with Rami). Header sent: X-Device-Key.
#define SECRET_DEVICE_KEY    "agree-this-value-with-rami"
